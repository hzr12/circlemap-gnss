name: Build Android APK (GNSS Plugin)

on:
  push:
    branches: [main]
    tags:
      - 'v*'
    paths:
      - 'native/**'
      - 'index.html'
      - 'js/**'
      - 'css/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build debug APK
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache npm dependencies
        uses: actions/cache@v4
        with:
          path: native/node_modules
          key: npm-${{ hashFiles('native/package-lock.json') }}
          restore-keys: npm-

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ hashFiles('native/android/**/*.gradle') }}
          restore-keys: gradle-

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Install native project dependencies
        working-directory: native
        run: npm install --no-audit --no-fund

      - name: Build GNSS plugin (TypeScript to JS)
        working-directory: native/gnss-plugin
        run: npx tsc

      - name: Add Android platform
        working-directory: native
        run: npx cap add android || true

      - name: Copy web assets to native/web/
        run: |
          mkdir -p native/web
          cp -R index.html js css native/web/
          ls -la native/web/
      - name: Sync web assets to Android project
        working-directory: native
        run: npx cap sync android

      - name: Integrate local GNSS plugin into Android project
        working-directory: native
        run: |
          # -- 1. Add module include to settings.gradle --
          echo "" >> android/settings.gradle
          echo "// GNSS data plugin" >> android/settings.gradle
          echo "include ':gnss-data-plugin'" >> android/settings.gradle
          echo "project(':gnss-data-plugin').projectDir = new File('../gnss-plugin/android')" >> android/settings.gradle

          # -- 2. Add dependency to app/build.gradle --
          sed -i "s|implementation project(':capacitor-android')|implementation project(':capacitor-android')\n    implementation project(':gnss-data-plugin')|" android/app/build.gradle

          echo "--- settings.gradle (tail) ---"
          tail -5 android/settings.gradle

          echo "--- app/build.gradle dependencies ---"
          grep -A2 "gnss-data-plugin" android/app/build.gradle || echo "(checking insert location)"

      - name: Ensure location permissions in app manifest
        working-directory: native
        run: |
          MANIFEST="android/app/src/main/AndroidManifest.xml"
          if ! grep -q "android.hardware.location.gps" "$MANIFEST"; then
            sed -i '/<\/manifest>/i \ \ \ \ <uses-feature android:name="android.hardware.location.gps" />' "$MANIFEST"
            echo "+ Added GPS feature to manifest"
          fi
          if ! grep -q "ACCESS_FINE_LOCATION" "$MANIFEST"; then
            sed -i '/<\/manifest>/i \ \ \ \ <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />' "$MANIFEST"
            echo "+ Added ACCESS_FINE_LOCATION to manifest"
          fi

      - name: Build debug APK
        working-directory: native/android
        run: ./gradlew assembleDebug --no-daemon

      - name: Sign APK with release keystore
        run: |
          KEYSTORE="native/circlemap.keystore"
          APK="native/android/app/build/outputs/apk/debug/app-debug.apk"
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            VERSION="${GITHUB_REF_NAME#v}"
          else
            VERSION="dev"
          fi
          APK_SIGNED="native/android/app/build/outputs/apk/debug/circlemap-v${VERSION}.apk"
          if [ ! -f "$APK" ]; then
            echo "APK not found, skipping sign"
            exit 0
          fi
          # Auto-detect latest build-tools version
          BT_DIR=$(ls "${ANDROID_HOME}/build-tools/" | sort -V | tail -1)
          echo "Using build-tools $BT_DIR"
          "${ANDROID_HOME}/build-tools/${BT_DIR}/zipalign" -p -f 4 "$APK" "$APK_SIGNED"
          "${ANDROID_HOME}/build-tools/${BT_DIR}/apksigner" sign \
            --ks "$KEYSTORE" \
            --ks-key-alias circlemap \
            --ks-pass pass:circlemap123 \
            --key-pass pass:circlemap123 \
            "$APK_SIGNED"
          echo "Signed APK: $APK_SIGNED"

      - name: Verify GNSS plugin classes in APK
        working-directory: native
        run: |
          APK="android/app/build/outputs/apk/debug/app-debug.apk"
          if [ ! -f "$APK" ]; then
            echo "APK not found at $APK, skipping verification"
            echo "::notice title=GNSS Verify::APK not found, skipping class verification"
            exit 0
          fi
          if unzip -l "$APK" 2>/dev/null | grep -qi "GnssData"; then
            echo "## GNSS plugin classes verified in APK" >> $GITHUB_STEP_SUMMARY
            echo "Found GnssData class in APK" | tee -a $GITHUB_STEP_SUMMARY
          else
            echo "## GNSS plugin classes NOT found in APK" >> $GITHUB_STEP_SUMMARY
            echo "The plugin may not have been linked into the build." >> $GITHUB_STEP_SUMMARY
            echo "Check the Integrate step logs for insertion errors." >> $GITHUB_STEP_SUMMARY
          fi

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: circlemap-gnss-debug
          path: native/android/app/build/outputs/apk/debug/circlemap-*.apk
          retention-days: 14

      - name: Publish to GitHub Release (tag push = release)
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          name: Circlemap ${{ github.ref_name }}
          body: |
            ## Circlemap ${{ github.ref_name }}

            ### 鍖呭惈
            - 鑵捐鍦板浘鍚屽績鍦嗭紙Canvas 鍙犲姞灞傦級
            - GNSS 鍗槦鏁版嵁鏄剧ず锛圙PS/鍖楁枟/GLONASS/Galileo锛?            - 杞ㄨ抗璁板綍 + GPX 瀵煎嚭
            - 娣辫壊/娴呰壊涓婚

            ### 瀹夎
            涓嬭浇 APK 鐩存帴瀹夎鍒?Android 璁惧锛堝凡绛惧悕锛屽皬绫崇瓑璁惧鍙洿鎺ュ畨瑁咃級銆?          files: native/android/app/build/outputs/apk/debug/circlemap-*.apk
          generate_release_notes: true

      - name: Publish dev build to release (branch push = dev build)
        if: github.ref_type == 'branch' && !startsWith(github.ref, 'refs/tags/v')
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          TAG="dev-build"
          APK_GLOB="native/android/app/build/outputs/apk/debug/circlemap-*.apk"
          APK_FILES=$(ls $APK_GLOB 2>/dev/null || true)
          if [ -z "$APK_FILES" ]; then
            echo "APK not found at $APK_GLOB, skipping dev release"
            exit 0
          fi
          gh release delete "$TAG" --yes 2>/dev/null || true
          gh release create "$TAG" \
            --target "${{ github.sha }}" \
            --title "Circlemap Dev Build (${{ github.sha }})" \
            --prerelease \
            --notes "鑷姩鏋勫缓锛宑ommit \`${{ github.sha }}\` 鍒嗘敮 \`${{ github.ref_name }}\`

            姝や负寮€鍙戠増棰勫彂甯冿紝鍙兘鍖呭惈鏈ǔ瀹氬姛鑳姐€?
            **鏋勫缓淇℃伅**
            - Commit: ${{ github.sha }}
            - 鍒嗘敮: ${{ github.ref_name }}
            - 瑙﹀彂: ${{ github.event_name }}" \
            $APK_FILES
          echo "Dev release updated"

      - name: Summarize APK info
        working-directory: native
        run: |
          APK_PATH="android/app/build/outputs/apk/debug"
          if [ -d "$APK_PATH" ]; then
            echo "## APK built successfully" >> $GITHUB_STEP_SUMMARY
            for f in "$APK_PATH"/circlemap-*.apk; do
              if [ -f "$f" ]; then
                SIZE=$(stat -c%s "$f" 2>/dev/null || stat -f%z "$f" 2>/dev/null)
                SIZE_MB=$(echo "scale=2; $SIZE/1048576" | bc)
                echo "- \`$(basename "$f")\` ${SIZE_MB}MB (宸茬鍚?" >> $GITHUB_STEP_SUMMARY
              fi
            done
          else
            echo "## APK not found" >> $GITHUB_STEP_SUMMARY
          fi
